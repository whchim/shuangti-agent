# 个人中心优化

**日期**: 2026-06-26

## 修改内容

### 1. 修复用户名修改校验BUG
- 修改用户名对话框新增 `.isalnum()` 校验，只允许英文字母+数字组合（纯字母/纯数字/混合均允许）
- 注册表单同步新增用户名格式校验
- 影响文件: `frontend/auth.py`

### 2. 账号切换后自动加载历史对话
- 侧边栏增加 `sessions_loaded` 判断，切换账号登录后自动拉取会话列表
- 影响文件: `frontend/app.py`

### 3. 注销后登录提示优化
- `login_user` 区分"用户不存在"与"密码错误"两种错误提示
- 影响文件: `app/service/auth_service.py`
